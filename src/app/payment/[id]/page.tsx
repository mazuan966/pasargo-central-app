
// This page is now obsolete as the payment flow is handled
// by a server action that redirects directly to Toyyibpay.
// The content is removed to prevent it from being used.
// It can be deleted in a future cleanup.
export default function DeprecatedPaymentPage() {
    return null;
}
